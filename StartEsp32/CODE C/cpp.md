## 🧱 أولًا: البنية الأساسية لأي كود ESP32 (أو Arduino عمومًا)

أي سكيتش (Sketch) لازم يحتوي على دالتين أساسيتين:

```C
#define Name NumberPin  // الجزء ده اسمه **Macro Definitions** أو **تعريفات مسبقة بالماكرو**

/*
قبل الكومبايلر ما يترجم الكود، في مرحلة اسمها 
**Preprocessing*
الجزء ده بيستبدل أي كلمة معرفّة بـ `#define` بقيمتها.
*/

void setup() {
  // هنا تكتب الأكواد اللي تتنفذ مرة واحدة فقط عند تشغيل الجهاز
}

void loop() {
  // هنا الأكواد اللي بتتكرر طول ما الجهاز شغال
}

```

---

## ⚙️ ثانيًا: الدوال الأساسية الجاهزة (بدون مكتبات)

دي الدوال المتاحة مباشرة من نواة ESP32/Arduino Core:

| الدالة                     | الوظيفة                               | مثال                           |
| -------------------------- | ------------------------------------- | ------------------------------ |
| `pinMode(pin, mode)`       | تعريف وضع البن (مدخل أو مخرج)         | `pinMode(2, OUTPUT);`          |
| `digitalWrite(pin, value)` | إرسال HIGH أو LOW لبن مخرج            | `digitalWrite(2, HIGH);`       |
| `digitalRead(pin)`         | قراءة حالة البن (HIGH أو LOW)         | `int x = digitalRead(14);`     |
| `analogRead(pin)`          | قراءة إشارة تماثلية (ADC) من البن     | `int sensor = analogRead(34);` |
| `analogWrite(pin, value)`  | توليد إشارة PWM (0 إلى 255)           | `analogWrite(2, 128);`         |
| `delay(ms)`                | إيقاف الكود لعدد من الملي ثواني       | `delay(1000);`                 |
| `millis()`                 | يرجع عدد الملي ثواني منذ تشغيل اللوحة | `unsigned long t = millis();`  |
| `micros()`                 | نفس فكرة `millis` لكن بالميكروثانية   | `unsigned long t = micros();`  |
| `delayMicroseconds(us)`    | تأخير بالميكروثانية                   | `delayMicroseconds(100);`      |
| `Serial.begin(baudrate)`   | بدء الاتصال التسلسلي عبر USB          | `Serial.begin(115200);`        |
| `Serial.print(value)`      | طباعة بيانات في الـ Serial Monitor    | `Serial.print("Hello");`       |
| `Serial.println(value)`    | نفس `print` بس بيضيف سطر جديد         | `Serial.println(123);`         |

```C
#define LED 2
#define BUTTON 14

void setup() {
  Serial.begin(115200);        // نبدأ السيريال  و علشان تعرف تطبع لازم تستخدم فاكشن هذه
  pinMode(LED, OUTPUT);        // البن 2 مخرج
  pinMode(BUTTON, INPUT);      // البن 14 مدخل
}

void loop() {
  int state = digitalRead(BUTTON); // نقرأ حالة الزر

  if (state == LOW) {
    digitalWrite(LED, HIGH);
    Serial.println("LED ON");
  } else {
    digitalWrite(LED, LOW);
    Serial.println("LED OFF");
  }

  delay(500);
}

```