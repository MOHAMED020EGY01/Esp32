# 🔎 شرح تفصيلي لبنات الجهة اليسرى (Left side pins)

---

### VIN — **إدخال 5V**

- **وظيفته:** نقطة دخل جهد تقدر توصل بيها 5V (من محول أو بطارية) علشان تُغذّي الـ onboard regulator (منظم الجهد) واللوحة كلها.
    
- **خواص كهربائية:** VIN → بيوصل إلى منظم الجهد على اللوحة، الناتج منه عادة 3.3V للـ ESP32.
    
    - **لا توصل أكثر من 5V** هنا إلا لو عارف اللوحة بتدعم.
        
- **متى تستخدمه:** لما تجيب 5V من مصدر خارجي (مثل شاحن USB لو موصل السلك من جهة مختلفة).
    
- **ملاحظة:** لو موصل USB عبر الـ microUSB فالـ VIN بيتغذى تلقائياً من الـ USB.
    

---

### GND — **الأرضي (Ground)**

- **وظيفته:** مرجع الجهد (0V). لازم توصل الأرضي بتاع أي حساس أو مصدر طاقة بالأرضي هنا.
    
- **نصيحة:** دايمًا اشبك كل GND مع بعض (لو في عدة مصادر) لتفادي إشارات غريبة.
  
  ---
  
  ### D13 (GPIO13)

- **وظيفته:** GPIO عام — تقدر تستخدمه كـ digital input أو output، وكذلك يدعم بعض البروتوكولات كـ SPI (SEC_PERIPH) حسب التكوين.
    
- **استخدامات عملية:** LED، زر، أو كجزء من واجهة SPI (اعتمادًا على الـ board mapping).
    
- **مثال تشغيل LED:**
    

```c
`pinMode(13, OUTPUT); 
digitalWrite(13, HIGH);
```

---

### D12 (GPIO12) — **تحذير: حساس أثناء الإقلاع**

- **لماذا حساس؟** GPIO12 مرتبط بإعدادات الـ boot strapping (تأثير على جهد الفلاش VDD_SDIO) في بعض إصدارات الـ ESP32. قيمة هذا البن أثناء إعادة التشغيل قد تؤثر على وضع الإقلاع أو على جهد محرك الفلاش.
    
- **متى تتجنب استخدامه؟** لو هتركب عليه مقاومات تسحب للـ HIGH/LOW أو هتعطيه إشارات عند البوت — الأفضل ما تستخدموش كإخراج أساسي أثناء عملية الإقلاع.
    
- **نصيحة عملية:** لو لازم تستخدمه، تأكد إن الحالة وقت الباور تكون متوافقة مع متطلبات البوت، أو استخدم بن بديل.
  
  ---
  
  ### D14 (GPIO14) — **GPIO + Touch Sensor (T6)**

- **وظيفته:** GPIO عادي ويُستخدم كبن حساس للمس (capacitive touch) اسمه T6.
    
- **ميزة اللمس:** تقدر تقيس لمسة بـ `touchRead(14)` في Arduino.
    
- **مثال لمس:**
  
```C
int val = touchRead(14);
Serial.println(val);
```

**ملاحظة:** قيم اللمس حسّاسة للتأريض والتوصيلات؛ استخدم سطح مناسب وحافظ على أسلاك قصيرة.

---
### D27 (GPIO27) — **GPIO + Touch Sensor (T7)**

- **وظيفته:** زي D14 — قابل للاستخدام كـ digital I/O وكمستشعر لمسي.
    
- **استخدام شائع:** أزرار لمس، إيقاظ الجهاز من وضع السكون برفق.
  
  ---
  
### D26 (GPIO26) — **GPIO + DAC (Analog Output)**

- **وظيفته:** يدعم **DAC** (إخراج تناظري فعلي) — واحد من بين اثنين في ESP32.
    
- **خواص:** تقدر تخرج قيمة analog (0..255 عادة في Arduino API) وتحصل على جهد مماثل بين 0V و ~3.3V.
    
- **مثال إخراج صوت/إشارة:**
  
```C
dacWrite(26, 128); // قيمة من 0 إلى 255

```  

- **استخدام عملي:** تشغيل نغمة بسيطة، تحكم إشارة تناظرية لمضخم صوت أو مرجح.
  
  ---
  ### D25 (GPIO25) — **GPIO + DAC**

- **وظيفته:** نفس وظيفة D26 — البَن التاني الداعم للـ DAC.
    
- **مثال:** `dacWrite(25, value);`
    
- **نصيحة:** لو هتستخدم DAC لإخراج صوت ضع مقاومة/فلتر أو استخدم مكبر مناسب لأن الـ DAC مباشرة قد يحتاج معالجة.
  
  ---
  ### D33 (GPIO33) — **GPIO + ADC**

- **وظيفته:** يقبل قراءة تناظرية (ADC). مناسب لقراءة قيم من حساسات جهد/نور/حرارة الخ.
    
- **نقطة مهمة:** GPIO33 يدعم ADC لكن غالباً ينتمي لـ ADC1 (ADC1 أكثر استقرارًا عند استخدام WiFi مقارنة بـ ADC2).
    
- **مثال قراءة analog:**
  
  ```C
int val = analogRead(33); // قيمة رقمية (افتراضي 0..4095 في Arduino core)
Serial.println(val);

  
```

- **نصيحة:** حافظ على الإشارات داخل 0..3.3V. لو الحساس يعطي أعلى، استخدم مقسم جهد.
---

### D32 (GPIO32) — **GPIO + ADC**

- **مهمة:** نفس كلام D33 — قراءة تناظرية جيدة للحساسات.
    
- **ملاحظة عملية:** استخدم ADC1 pins (32..39) لقراءات أكثر استقرارًا أثناء تشغيل Wi-Fi.
  
  ---

### D35 (GPIO35) — **Input Only + ADC**

- **وظيفته:** مدخل **تناظري فقط** — لا يدعم digital output.
    
- **تحديد:** لا ينفع تستخدمه لإخراج إشارات أو PWM.
    
- **مثال قراءة:**
  ```C
  int v = analogRead(35);
Serial.println(v);

```  

- **نصيحة:** ممتاز لقراءات حساسات فقط — تأكد إن ما تحاولش تعطيه خروج لأن مش هينفع.
---

### D34 (GPIO34) — **Input Only + ADC**

- **زي D35:** مدخل تناظري فقط، مناسب لقياس أنظمة الاستشعار.
  
  ---
  
  ### VN / VP (GPIO36 / GPIO39) — **ADC Channels (Input only)**

- **الاسماء الشائعة:** VP = GPIO36, VN = GPIO39. دول جزء من مجموعة البنات الخاصة بالـ ADC (غالبًا ADC1).
    
- **خواص:** **Input only**، لا يقدِمان إخراج.
    
- **استخدام عملي:** قياسات حسّاسة، غالبًا تُستخدم لقراءة إشارات استشعار مباشرة.
    
- **ملاحظة:** على بعض لوحات DevKit قد يكون اسمهم مطبوع كـ VP/VN بدل الأرقام.
  
  ---
  
  ### EN — **Enable (Reset/Enable pin)**

- **وظيفته:** زر تشغيل/إيقاف للـ ESP32.
    
    - لو عملته **LOW** الجهاز بيتوقف (يشبه Reset).
        
    - عادة الواحد بيوصله بزر Reset قصير للـ board.
        
- **متى تستخدمه برمجيًا/هاردويرياً:** لو عايز تعمل power-cycle خارجي أو reset من دائرة خارجية.
    
- **نصيحة:** ما تخشله إشارة عشوائية — لو هتحكم فيه من دائرة، ضيف مقاومة سحب (pull-up) لضمان ما يكونش LOW بالخطأ.
  
  